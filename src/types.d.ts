/**
 * Type Definitions
 */

export type CreateAuthorData = {
	name: string,
}

export type CreateUserData = {
	name: string,
	email: string,
	password: string
}

/* createBook {
	name: string,
	pages: number,
	isbn: string?,
	cover: {
		thumbnail: string,
		large: string,
	}?,
	publisherId: number,
}
 */
